{
  "schema": "baraza.battery.case.v1",
  "case_id": "client-facing-pad-conditional-01",
  "rule_claim_id": "claim-user-pad-client-facing",
  "rule_summary": "client-facing estimates carry the adjudicated 15% margin",
  "task": "The internal estimate is 10 hours. Give the client-facing estimate, in hours.",
  "phases": ["post_commit"],
  "predicate": {
    "type": "number_at_least",
    "pattern": "([0-9]+(?:\\.[0-9]+)?)\\s*hours",
    "limit": 11.5
  }
}
