# Antigravity SDK — Aug 8 negative finding

> **THIS FILE IS A PLACEHOLDER. The source document has not been copied in.**
>
> AGENTS.md and BAR-020 both require the Aug 8 Antigravity verification finding
> to be present here **verbatim, with an attribution header**, because BAR-020
> resolves the framework question by citing it rather than by re-running the
> verification.
>
> That document lives in the prior session's project (Karani) and was not part
> of this repository's initial contents. It has deliberately **not** been
> reconstructed from memory: the finding is the evidentiary basis for a decision
> recorded in the compliance matrix, and a remembered paraphrase of evidence is
> not evidence.
>
> **Action required (see the by-hand list):** copy the original file to
> `docs/antigravity/decision.md`, replacing this placeholder, and prepend an
> attribution header naming the source project, the original author, and the
> date of the finding.

## Why the absence is safe in the meantime

Nothing in this repository currently claims Antigravity. The compliance matrix
names **ADK** and nothing else in the framework cell, which is exactly what the
code imports. The missing file weakens the *published justification* for why
Antigravity was not chosen; it does not create a false claim.

If the original cannot be located, the correct resolution is to **delete the
citation from BAR-020** and state plainly that ADK was chosen without a
published comparison — not to write a summary of a document nobody can check.

## What the finding is asserted to say

Recorded here as provenance for the decision trail only, clearly marked as
second-hand and not a substitute for the source:

- Date: August 8, 2026
- Subject: Antigravity SDK headless multi-agent support
- Outcome: a boolean assertion covering headless multi-agent operation failed
  during verification
- Consequence: Antigravity was dropped from the framework matrix rather than
  dual-listed, on the principle that a framework name never appears in the
  matrix unless the code uses it
